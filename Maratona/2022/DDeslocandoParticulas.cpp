#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    int x, y;
    cin >> n >> x >> y;

    int edge = 1 << n;
    while (x != edge ) {
        
    }
    cout << actv << endl;
}