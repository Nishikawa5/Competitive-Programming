#include <bits/stdc++.h>
using namespace std;

/*
Printar o resultado apos ler todas as entradas?
*/
int main(void) {
    int bit;
    bool fl = true;
    for (int i = 0 ; i < 8; i++) {
        cin >> bit;
        if (bit == 9) {
            fl = false;
        cout << "F" << endl;
break;
        }
    }
    if (fl) {
        cout << "S" << endl;
    } else {
    }
}