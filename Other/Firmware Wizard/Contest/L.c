#include <stdio.h>
#include <stdlib.h>
#include <string.h>
 
int max(int a, int b) {
    return (a < b ? b : a);
}



int main() {
    int n;
    char moleculas[n][8];
    int visited[n];
    memset(visited, 0, sizoef(visited));

    for (int i = 0; i < )


    return 0;
}