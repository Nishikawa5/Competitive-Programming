#include <bits/stdc++.h>

using namespace std;

#define point pair<int, int>
#define line pair<point, point>

/*
make some structure containing all intersection points and see how many of these points are inside
the rectangle at some coordinate
*/
int main(void) {
    int m, n;
    int len;
    int lines;

    scanf("%d %d", &m, &n);
    scanf("%d", &len);
    scanf("%d", &lines);

    vector<point> 

    for (int i = 0; i < lines; i++) {
        int x1, y1, x2, y2;
        scanf
    }

}