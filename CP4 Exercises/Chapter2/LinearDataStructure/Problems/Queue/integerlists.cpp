#include <bits/stdc++.h>
using namespace std;

int main() {
    int tc;
    cin >> tc;

    while (tc--) {
        int n;
        cin >> n;
        getchar();

        deque<int> list;
        while (n--) {
            int num;
            cin >> num;
            list.push_back(num);

            getchar();
        }

        char dir = 'B';
    }
}